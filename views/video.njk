{% extends "layout.njk"%}



{%  block head %}  

<title> {{item.title}}-rocketseat </title>

{% endblock %}


{% block content %}

        <div id="wrapper"  class="videos">
          <h1>{{item.title}}</h1>
            <!---  h1 até o h6 são ideias para trabalhar com titulo-->
        </div>
        


        <section class="videos">
                          <div class="card video" id="{{item.id}}">
                  <div class="card_video " >
                    <iframe src="https://www.youtube.com/embed/{{item.id}}" frameborder="0">
                        </iframe>
           
                    </div>

                    <div class="cardCon">

                      <p>{{item.title}}</p>
                    </div>

                    <div class="info">
                      <p>{{item.duration}}</p>
                      <div class="price">
                        <P>{{item.price}}</P>
                      </div>
                    </div>
                </div>
              
        </section>


      {% endblock %}
